# Cloudflare Pages secret variables — set via dashboard (Settings → Environment variables)
# NEVER commit the actual .env file. This file is a reference only.

# Resend API key — create at https://resend.com/api-keys
# Free tier: 100 emails/day
RESEND_API_KEY=

# Your email — where contact form submissions are sent
CONTACT_EMAIL=eduardomarcelo03@gmail.com

# GitHub token (optional) — for higher API rate limit on prebuild script
# GITHUB_TOKEN=
